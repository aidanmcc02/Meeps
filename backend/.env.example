# Backend environment – copy to .env in this folder if you run backend from here.
# Or use the root .env (backend also loads ../../.env when run from backend/).
PORT=4000
NODE_ENV=development
JWT_SECRET=your_dev_jwt_secret_change_me
# GIPHY_API_KEY=

# Diana webhook – match updates posted to #matches (Diana sends POST with this secret)
# DIANA_WEBHOOK_SECRET=your_shared_secret

# PostgreSQL – local (matches project docker-compose.yml)
PGHOST=localhost
PGPORT=5433
PGDATABASE=meeps
PGUSER=postgres
PGPASSWORD=postgres
# Or: DATABASE_URL=postgresql://postgres:postgres@localhost:5433/meeps
