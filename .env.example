# Backend URLs baked into the Tauri app at build time.
# Copy to .env and set your values, then run: npm run tauri build

# Production API (HTTPS) — must include protocol or requests get app HTML and break JSON parsing
VITE_BACKEND_HTTP_URL=https://meeps-production.up.railway.app
VITE_BACKEND_WS_URL=https://meeps-production.up.railway.app/ws

# Diana bot API (for AI/games features)
VITE_DIANA_API_URL=https://diana-bot-production.up.railway.app

# Optional: secret for GitHub Actions to post build results to the Builds channel (set in repo Secrets as BUILD_WEBHOOK_SECRET)
# BUILD_WEBHOOK_SECRET=your-secret-here

# Backend: directory for uploaded files (use Railway volume mount path in production, e.g. /data or RAILWAY_VOLUME_MOUNT_PATH/uploads)
# UPLOADS_PATH=./uploads

# Backend: Web Push (mention notifications when app is closed, e.g. iOS PWA). Generate with: cd backend && node scripts/generate-vapid-keys.js
# VAPID_PUBLIC_KEY=...
# VAPID_PRIVATE_KEY=...
