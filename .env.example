# Backend URLs baked into the Tauri app at build time.
# Copy to .env and set your values, then run: npm run tauri build

# Production API (HTTPS) — must include protocol or requests get app HTML and break JSON parsing
VITE_BACKEND_HTTP_URL=https://meeps-production.up.railway.app
VITE_BACKEND_WS_URL=https://meeps-production.up.railway.app/ws
